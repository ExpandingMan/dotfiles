# try harder to keep connection alive
Host *
    ServerAliveInterval 60

# use the setup below if you need to do a jump
#Host servername
#    User expandingman
#    ForwardAgent yes
#    IdentityFile ~/.ssh/id_servername
#    ProxyJump jumpserver
#    # use the below instead of proxy jumps on old systems
#    # ProxyCommand ssh -W %h:%p jumpserver

# Note that the alias for the ssh command is the string to the right of Host

# sample config
#Host servername2
#    HostName servername2
#    IdentityFile ~/.ssh/id_privatekey
#    PreferredAuthentications publickey
